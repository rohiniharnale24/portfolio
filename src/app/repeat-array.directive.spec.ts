import { RepeatArrayDirective } from './repeat-array.directive';

describe('RepeatArrayDirective', () => {
  it('should create an instance', () => {
    const directive = new RepeatArrayDirective();
    expect(directive).toBeTruthy();
  });
});
